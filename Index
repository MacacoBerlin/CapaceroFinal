<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Capa Cero · Diagnóstico de Soberanía</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Syne:wght@400;600;800&display=swap" rel="stylesheet">
<style>
  /* ── RESET & VARIABLES ── */
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --bg:        #0a0c0f;
    --surface:   #111418;
    --surface2:  #181c22;
    --border:    #1e2530;
    --accent:    #00e5a0;       /* verde soberanía */
    --danger:    #ff3b3b;       /* rojo fuga */
    --warn:      #f59e0b;       /* ámbar */
    --muted:     #4a5568;
    --text:      #e2e8f0;
    --text-dim:  #718096;
    --mono:      'Space Mono', monospace;
    --sans:      'Syne', sans-serif;
    --glow-green: 0 0 20px rgba(0,229,160,.25);
    --glow-red:   0 0 20px rgba(255,59,59,.25);
  }

  html { scroll-behavior: smooth; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: var(--sans);
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* ── GRID BACKGROUND ── */
  body::before {
    content: '';
    position: fixed; inset: 0;
    background-image:
      linear-gradient(rgba(0,229,160,.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0,229,160,.03) 1px, transparent 1px);
    background-size: 40px 40px;
    pointer-events: none;
    z-index: 0;
  }

  /* ── LAYOUT ── */
  .wrap {
    position: relative; z-index: 1;
    max-width: 780px;
    margin: 0 auto;
    padding: 0 20px 80px;
  }

  /* ── HEADER ── */
  header {
    padding: 48px 0 32px;
    border-bottom: 1px solid var(--border);
    margin-bottom: 48px;
    animation: fadeDown .6s ease both;
  }

  .logo-line {
    display: flex; align-items: center; gap: 12px;
    margin-bottom: 12px;
  }

  .logo-badge {
    background: var(--accent);
    color: var(--bg);
    font-family: var(--mono);
    font-size: 10px;
    font-weight: 700;
    padding: 3px 8px;
    letter-spacing: 2px;
  }

  .status-dot {
    width: 8px; height: 8px;
    background: var(--accent);
    border-radius: 50%;
    box-shadow: var(--glow-green);
    animation: pulse 2s infinite;
  }

  h1 {
    font-size: clamp(28px, 5vw, 44px);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -1px;
  }

  h1 span { color: var(--accent); }

  .subtitle {
    margin-top: 10px;
    color: var(--text-dim);
    font-family: var(--mono);
    font-size: 13px;
    letter-spacing: 1px;
  }

  /* ── SECCIONES · scroll-reveal ── */
  section {
    margin-bottom: 48px;
    opacity: 0;
    transform: translateY(28px);
    transition: opacity .55s ease, transform .55s ease;
  }

  section.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Header sigue con CSS animation directa */
  header { opacity: 1; transform: none; }

  .section-label {
    font-family: var(--mono);
    font-size: 11px;
    color: var(--accent);
    letter-spacing: 3px;
    text-transform: uppercase;
    margin-bottom: 16px;
    display: flex; align-items: center; gap: 10px;
  }

  .section-label::after {
    content: '';
    flex: 1;
    height: 1px;
    background: var(--border);
  }

  /* ── PANEL (TARJETA BASE) ── */
  .panel {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 24px;
  }

  .panel-title {
    font-size: 13px;
    font-family: var(--mono);
    color: var(--text-dim);
    margin-bottom: 8px;
    letter-spacing: 1px;
  }

  /* ── INPUTS ── */
  .grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
  .grid-3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 16px; }

  @media (max-width: 520px) {
    .grid-2, .grid-3 { grid-template-columns: 1fr; }
  }

  .field { display: flex; flex-direction: column; gap: 8px; }

  label {
    font-size: 11px;
    font-family: var(--mono);
    color: var(--text-dim);
    letter-spacing: 1.5px;
    text-transform: uppercase;
  }

  input[type="number"], select {
    background: var(--surface2);
    border: 1px solid var(--border);
    color: var(--text);
    font-family: var(--mono);
    font-size: 20px;
    padding: 12px 16px;
    border-radius: 2px;
    width: 100%;
    outline: none;
    transition: border-color .2s, box-shadow .2s;
    -moz-appearance: textfield;
  }

  input[type="number"]::-webkit-inner-spin-button { -webkit-appearance: none; }

  input[type="number"]:focus, select:focus {
    border-color: var(--accent);
    box-shadow: 0 0 0 3px rgba(0,229,160,.1);
  }

  select { font-size: 14px; cursor: pointer; }
  select option { background: var(--surface2); }

  /* ── SLIDER ── */
  .slider-wrap { display: flex; flex-direction: column; gap: 10px; }

  input[type="range"] {
    -webkit-appearance: none;
    width: 100%;
    height: 4px;
    background: var(--border);
    border-radius: 2px;
    outline: none;
    cursor: pointer;
  }

  input[type="range"]::-webkit-slider-thumb {
    -webkit-appearance: none;
    width: 18px; height: 18px;
    background: var(--accent);
    border-radius: 50%;
    box-shadow: var(--glow-green);
    transition: transform .15s;
  }

  input[type="range"]::-webkit-slider-thumb:hover { transform: scale(1.3); }

  .slider-labels {
    display: flex; justify-content: space-between;
    font-family: var(--mono); font-size: 10px; color: var(--muted);
  }

  /* ── PROTOCOLO SELECTOR ── */
  .proto-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }

  .proto-btn {
    background: var(--surface2);
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 16px;
    cursor: pointer;
    transition: border-color .2s, background .2s;
    text-align: left;
  }

  .proto-btn:hover { border-color: var(--muted); }

  .proto-btn.selected {
    border-color: var(--accent);
    background: rgba(0,229,160,.06);
    box-shadow: var(--glow-green);
  }

  .proto-btn.selected-danger {
    border-color: var(--danger);
    background: rgba(255,59,59,.06);
    box-shadow: var(--glow-red);
  }

  .proto-icon { font-size: 22px; margin-bottom: 6px; }
  .proto-name { font-family: var(--mono); font-size: 13px; font-weight: 700; color: var(--text); }
  .proto-desc { font-size: 11px; color: var(--text-dim); margin-top: 4px; line-height: 1.4; }

  /* ── MÉTRICAS VITALES ── */
  .vitals {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    background: var(--border);
    border: 1px solid var(--border);
    border-radius: 4px;
    overflow: hidden;
  }

  @media (max-width: 520px) { .vitals { grid-template-columns: 1fr; } }

  .vital {
    background: var(--surface);
    padding: 20px;
    transition: background .2s;
  }

  .vital:hover { background: var(--surface2); }

  .vital-label {
    font-family: var(--mono);
    font-size: 10px;
    color: var(--text-dim);
    letter-spacing: 2px;
    text-transform: uppercase;
    margin-bottom: 8px;
  }

  .vital-value {
    font-family: var(--mono);
    font-size: 28px;
    font-weight: 700;
    line-height: 1;
    transition: color .4s;
  }

  .vital-unit {
    font-family: var(--mono);
    font-size: 11px;
    color: var(--text-dim);
    margin-top: 4px;
  }

  .color-green { color: var(--accent); }
  .color-red   { color: var(--danger); }
  .color-warn  { color: var(--warn); }
  .color-dim   { color: var(--text-dim); }

  /* ── CONTADOR TICKER ── */
  .ticker-wrap {
    background: var(--surface);
    border: 1px solid var(--danger);
    border-radius: 4px;
    padding: 28px 24px;
    text-align: center;
    box-shadow: var(--glow-red);
    position: relative;
    overflow: hidden;
  }

  .ticker-wrap::before {
    content: '';
    position: absolute; inset: 0;
    background: linear-gradient(180deg, rgba(255,59,59,.04) 0%, transparent 100%);
  }

  .ticker-label {
    font-family: var(--mono);
    font-size: 10px;
    color: var(--danger);
    letter-spacing: 3px;
    margin-bottom: 12px;
    opacity: .8;
  }

  .ticker-amount {
    font-family: var(--mono);
    font-size: clamp(36px, 8vw, 56px);
    font-weight: 700;
    color: var(--danger);
    text-shadow: var(--glow-red);
    letter-spacing: -1px;
    line-height: 1;
  }

  .ticker-sub {
    font-family: var(--mono);
    font-size: 12px;
    color: var(--muted);
    margin-top: 10px;
  }

  /* ── BARRA DE VEREDICTO ── */
  .verdict {
    border-radius: 4px;
    padding: 20px 24px;
    font-family: var(--mono);
    font-size: 13px;
    line-height: 1.6;
    border-left: 4px solid;
  }

  .verdict-danger { background: rgba(255,59,59,.08); border-color: var(--danger); color: var(--danger); }
  .verdict-warn   { background: rgba(245,158,11,.08); border-color: var(--warn); color: var(--warn); }
  .verdict-ok     { background: rgba(0,229,160,.08); border-color: var(--accent); color: var(--accent); }

  /* ── GRÁFICA ── */
  .chart-area {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 24px;
    position: relative;
  }

  .chart-legend {
    display: flex; gap: 20px;
    font-family: var(--mono); font-size: 11px;
    margin-bottom: 16px;
  }

  .legend-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    display: inline-block;
    margin-right: 6px;
    vertical-align: middle;
  }

  canvas#chart { width: 100%; height: 220px; display: block; }

  /* ── CRUCE ROI ── */
  .roi-box {
    background: rgba(0,229,160,.06);
    border: 1px solid var(--accent);
    border-radius: 4px;
    padding: 20px 24px;
    display: flex; align-items: center; gap: 16px;
    box-shadow: var(--glow-green);
    margin-top: 16px;
  }

  .roi-icon { font-size: 28px; flex-shrink: 0; }

  .roi-text { font-family: var(--mono); font-size: 13px; line-height: 1.6; color: var(--accent); }

  /* ── INVENTARIO ── */
  .inventory { display: flex; flex-direction: column; gap: 10px; }

  .inv-item {
    display: grid;
    grid-template-columns: 36px 1fr auto;
    align-items: center;
    gap: 12px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 14px 18px;
    transition: border-color .2s;
  }

  .inv-item:hover { border-color: var(--muted); }

  .inv-emoji { font-size: 18px; text-align: center; }

  .inv-name { font-size: 14px; color: var(--text); }
  .inv-note { font-family: var(--mono); font-size: 11px; color: var(--text-dim); margin-top: 2px; }

  .inv-qty {
    font-family: var(--mono);
    font-size: 20px;
    font-weight: 700;
    color: var(--accent);
    text-align: right;
    min-width: 28px;
  }

  /* ── GLOSARIO ── */
  .glossary { display: flex; flex-direction: column; gap: 10px; }

  .glos-item {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 16px 18px;
    cursor: pointer;
    transition: border-color .2s;
  }

  .glos-item:hover { border-color: var(--accent); }

  .glos-term {
    font-family: var(--mono);
    font-size: 13px;
    color: var(--accent);
    font-weight: 700;
    display: flex; justify-content: space-between; align-items: center;
  }

  .glos-def {
    font-size: 13px;
    color: var(--text-dim);
    line-height: 1.6;
    margin-top: 8px;
    display: none;
  }

  .glos-item.open .glos-def { display: block; }
  .glos-item.open { border-color: var(--accent); background: rgba(0,229,160,.04); }

  .caret { transition: transform .2s; font-size: 10px; color: var(--muted); }
  .glos-item.open .caret { transform: rotate(180deg); }

  /* ── CTA FINAL ── */
  .cta-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
  @media (max-width: 480px) { .cta-grid { grid-template-columns: 1fr; } }

  .btn {
    padding: 16px 24px;
    border-radius: 4px;
    font-family: var(--mono);
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 1px;
    cursor: pointer;
    border: none;
    transition: transform .15s, box-shadow .15s;
    text-decoration: none;
    display: block;
    text-align: center;
    text-transform: uppercase;
  }

  .btn:hover { transform: translateY(-2px); }
  .btn:active { transform: translateY(0); }

  .btn-primary {
    background: var(--accent);
    color: var(--bg);
    box-shadow: var(--glow-green);
  }

  .btn-primary:hover { box-shadow: 0 0 30px rgba(0,229,160,.5); }

  .btn-ghost {
    background: transparent;
    color: var(--accent);
    border: 1px solid var(--accent);
  }

  /* ── FOOTER ── */
  footer {
    border-top: 1px solid var(--border);
    padding: 24px 0;
    text-align: center;
    font-family: var(--mono);
    font-size: 11px;
    color: var(--muted);
    letter-spacing: 1px;
  }

  /* ── ANIMACIONES ── */
  @keyframes fadeDown {
    from { opacity: 0; transform: translateY(-20px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; box-shadow: 0 0 6px rgba(0,229,160,.6); }
    50%       { opacity: .4; box-shadow: 0 0 14px rgba(0,229,160,.3); }
  }

  @keyframes scanline {
    from { transform: translateY(-100%); }
    to   { transform: translateY(100vh); }
  }

  /* Scanline overlay sutil */
  body::after {
    content: '';
    position: fixed; inset: 0;
    background: linear-gradient(transparent 50%, rgba(0,0,0,.015) 50%);
    background-size: 100% 4px;
    pointer-events: none;
    z-index: 0;
  }
</style>
</head>
<body>
<div class="wrap">

  <!-- ── HEADER ── -->
  <header>
    <div class="logo-line">
      <span class="logo-badge">CAPA CERO</span>
      <div class="status-dot"></div>
      <span style="font-family:var(--mono);font-size:11px;color:var(--text-dim)">SISTEMA OPERATIVO</span>
    </div>
    <h1>Diagnóstico de<br><span>Soberanía del Hogar</span></h1>
    <p class="subtitle">// tu casa te habla. ¿estás escuchando?</p>
  </header>

  <!-- ── ACTO I: CONFIGURACIÓN ── -->
  <section>
    <div class="section-label">01 · PARÁMETROS DE ENTRADA</div>
    <div class="panel" style="display:flex;flex-direction:column;gap:20px;">

      <div class="grid-2">
        <div class="field">
          <label>Factura mensual (€)</label>
          <input type="number" id="factura" value="120" min="20" max="800" step="5">
        </div>
        <div class="field">
          <label>Número de estancias</label>
          <input type="number" id="estancias" value="3" min="1" max="10">
        </div>
      </div>

      <div class="slider-wrap">
        <label>Inflación energética anual: <span id="inf-val" style="color:var(--accent);font-family:var(--mono)">9%</span></label>
        <input type="range" id="inflacion" min="0" max="20" value="9" step="1">
        <div class="slider-labels">
          <span>0% · utopía</span>
          <span>10% · probable</span>
          <span>20% · crisis</span>
        </div>
      </div>

      <div class="field">
        <label>Nivel de automatización actual</label>
        <select id="nivel">
          <option value="0">Sin automatizar — enchufes normales, nada</option>
          <option value="0.20">Básico — alguna bombilla o enchufe inteligente</option>
          <option value="0.45" selected>Intermedio — varios dispositivos, aún con apps de marca</option>
          <option value="0.75">Soberanía Total — Home Assistant local, Zigbee, sin cloud</option>
        </select>
      </div>

      <div>
        <label style="display:block;margin-bottom:12px;">Protocolo de comunicación</label>
        <div class="proto-grid">
          <div class="proto-btn selected-danger" id="proto-wifi" onclick="setProto('wifi')">
            <div class="proto-icon">☁️</div>
            <div class="proto-name">Wi-Fi · Cloud</div>
            <div class="proto-desc">Tus datos salen de casa. Amazon, Google o el fabricante de turno saben lo que haces y cuándo.</div>
          </div>
          <div class="proto-btn" id="proto-zigbee" onclick="setProto('zigbee')">
            <div class="proto-icon">🛡️</div>
            <div class="proto-name">Zigbee / Thread · Local</div>
            <div class="proto-desc">Todo en casa. Sin intermediarios. Si internet cae, tus luces siguen funcionando.</div>
          </div>
        </div>
      </div>

    </div>
  </section>

  <!-- ── ACTO II: VITALES ── -->
  <section>
    <div class="section-label">02 · VITALES DEL SISTEMA</div>

    <div class="vitals" style="margin-bottom:16px;">
      <div class="vital">
        <div class="vital-label">Factura actual</div>
        <div class="vital-value color-dim" id="v-actual">120</div>
        <div class="vital-unit">€ / mes</div>
      </div>
      <div class="vital">
        <div class="vital-label">Con inflación (año 1)</div>
        <div class="vital-value color-warn" id="v-inflada">131</div>
        <div class="vital-unit">€ / mes</div>
      </div>
      <div class="vital">
        <div class="vital-label">Ahorro potencial</div>
        <div class="vital-value color-green" id="v-ahorro">59</div>
        <div class="vital-unit">€ / mes</div>
      </div>
    </div>

    <div id="verdict-box" class="verdict verdict-warn">
      Calculando diagnóstico...
    </div>
  </section>

  <!-- ── CONTADOR ── -->
  <section>
    <div class="section-label">03 · CONTADOR DE FUGA EN CURSO</div>

    <!-- Toggle modo -->
    <div style="display:flex;align-items:center;gap:0;margin-bottom:16px;border:1px solid var(--border);border-radius:4px;overflow:hidden;width:fit-content;">
      <button id="btn-modo-fuga" onclick="setModoContador('fuga')"
        style="padding:10px 20px;font-family:var(--mono);font-size:11px;letter-spacing:1px;cursor:pointer;border:none;
               background:var(--danger);color:#fff;transition:background .2s,color .2s;">
        ☁️ SIN SOBERANÍA
      </button>
      <button id="btn-modo-sober" onclick="setModoContador('soberania')"
        style="padding:10px 20px;font-family:var(--mono);font-size:11px;letter-spacing:1px;cursor:pointer;border:none;
               background:var(--surface2);color:var(--text-dim);transition:background .2s,color .2s;">
        🛡️ CON SOBERANÍA
      </button>
    </div>

    <div id="ticker-wrap-el" class="ticker-wrap">
      <div class="ticker-label" id="ticker-label">⬤ CAPITAL ESCAPANDO EN TIEMPO REAL</div>
      <div class="ticker-amount">€ <span id="ticker">0.000000</span></div>
      <div class="ticker-sub" id="ticker-sub">— €/mes · — €/año</div>
      <!-- barra de diferencia -->
      <div style="margin-top:18px;padding-top:16px;border-top:1px solid rgba(255,255,255,.07);">
        <div style="font-family:var(--mono);font-size:10px;color:var(--muted);letter-spacing:2px;margin-bottom:8px;">
          COMPARATIVA MENSUAL
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;">
          <div style="background:rgba(255,59,59,.08);border:1px solid rgba(255,59,59,.3);border-radius:3px;padding:10px;text-align:center;">
            <div style="font-family:var(--mono);font-size:10px;color:var(--danger);letter-spacing:1px;margin-bottom:4px;">SIN SOBERANÍA</div>
            <div style="font-family:var(--mono);font-size:18px;color:var(--danger);" id="comp-fuga">—</div>
            <div style="font-family:var(--mono);font-size:10px;color:var(--muted);">€/mes</div>
          </div>
          <div style="background:rgba(0,229,160,.06);border:1px solid rgba(0,229,160,.3);border-radius:3px;padding:10px;text-align:center;">
            <div style="font-family:var(--mono);font-size:10px;color:var(--accent);letter-spacing:1px;margin-bottom:4px;">CON SOBERANÍA</div>
            <div style="font-family:var(--mono);font-size:18px;color:var(--accent);" id="comp-sober">—</div>
            <div style="font-family:var(--mono);font-size:10px;color:var(--muted);">€/mes</div>
          </div>
        </div>
        <div style="margin-top:10px;background:var(--surface2);border-radius:3px;overflow:hidden;height:6px;">
          <div id="diff-bar" style="height:100%;background:linear-gradient(90deg,var(--danger),var(--accent));width:50%;transition:width .8s ease;"></div>
        </div>
        <div style="font-family:var(--mono);font-size:10px;color:var(--text-dim);margin-top:6px;text-align:center;" id="diff-label">
          Diferencia: — €/mes
        </div>
      </div>
    </div>
  </section>

  <!-- ── GRÁFICA ── -->
  <section>
    <div class="section-label">04 · PROYECCIÓN 10 AÑOS</div>
    <div class="chart-area">
      <div class="chart-legend">
        <span><span class="legend-dot" style="background:var(--danger)"></span>Inacción (seguir igual)</span>
        <span><span class="legend-dot" style="background:var(--accent)"></span>Inversión Soberana</span>
      </div>
      <canvas id="chart"></canvas>
    </div>
    <div id="roi-box" class="roi-box">
      <div class="roi-icon">⚡</div>
      <div class="roi-text" id="roi-text">Calculando punto de cruce...</div>
    </div>
  </section>

  <!-- ── INVENTARIO ── -->
  <section>
    <div class="section-label">05 · INVENTARIO NECESARIO</div>
    <div class="inventory" id="inventory">
      <!-- generado por JS -->
    </div>
  </section>

  <!-- ── NODO LOCAL ── -->
  <section>
    <div class="section-label">06 · ESTADO DEL NODO · MICASA</div>
    <div class="panel" style="display:grid;grid-template-columns:1fr 1fr;gap:16px;align-items:start;">
      <div>
        <div style="background:rgba(245,158,11,.08);border:1px solid var(--warn);border-radius:4px;padding:20px;text-align:center;">
          <div style="font-size:32px;margin-bottom:8px;">🏗️</div>
          <div style="font-family:var(--mono);font-size:11px;color:var(--warn);letter-spacing:2px;">INSTALACIÓN EN CURSO</div>
          <div style="font-size:11px;color:var(--text-dim);margin-top:6px;">Fase 1 de 3</div>
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:12px;">
          <div style="background:rgba(0,229,160,.06);border:1px solid var(--border);border-radius:4px;padding:12px;">
            <div style="font-family:var(--mono);font-size:10px;color:var(--accent);letter-spacing:1px;">📶 ZIGBEE 3.0</div>
            <div style="font-size:11px;color:var(--text-dim);margin-top:4px;">Datos 100% locales</div>
          </div>
          <div style="background:rgba(0,229,160,.06);border:1px solid var(--border);border-radius:4px;padding:12px;">
            <div style="font-family:var(--mono);font-size:10px;color:var(--accent);letter-spacing:1px;">⚡ LOCAL PUSH</div>
            <div style="font-size:11px;color:var(--text-dim);margin-top:4px;">Respuesta instantánea</div>
          </div>
        </div>
      </div>
      <div style="font-size:13px;color:var(--text-dim);line-height:1.7;">
        <p style="color:var(--text);font-weight:600;margin-bottom:8px;">¿Qué estamos montando?</p>
        <p>Una <strong style="color:var(--accent)">Raspberry Pi 3</strong> con Home Assistant OS. El cerebro que no duerme, no cobra suscripción y no le manda tus hábitos a nadie.</p>
        <br>
        <p>Cuando esté operativo, el contador de arriba <strong style="color:var(--accent)">se ralentiza drásticamente</strong>.</p>
        <div style="margin-top:14px;background:var(--surface2);border-radius:3px;overflow:hidden;">
          <div style="background:var(--accent);height:4px;width:35%;transition:width 1s ease;"></div>
        </div>
        <div style="font-family:var(--mono);font-size:10px;color:var(--muted);margin-top:6px;">PROGRESO: 35% · Fase hardware completada</div>
      </div>
    </div>
  </section>

  <!-- ── GLOSARIO ── -->
  <section>
    <div class="section-label">07 · DICCIONARIO DE SUPERVIVENCIA</div>
    <div class="glossary" id="glossary">
      <!-- generado por JS -->
    </div>
  </section>

  <!-- ── CTA ── -->
  <section>
    <div class="section-label">08 · SIGUIENTE MOVIMIENTO</div>
    <div style="background:var(--surface);border:1px solid var(--border);border-radius:4px;padding:28px;margin-bottom:16px;text-align:center;">
      <div style="font-size:13px;color:var(--text-dim);font-family:var(--mono);margin-bottom:8px;">AHORRO ACUMULADO EN 10 AÑOS</div>
      <div style="font-family:var(--mono);font-size:clamp(32px,6vw,48px);font-weight:700;color:var(--accent);" id="big-saving">—</div>
      <div style="font-size:13px;color:var(--text-dim);margin-top:8px;">con inversión inicial estimada de <span id="inv-estimada" style="color:var(--text)">—</span></div>
    </div>
    <div class="cta-grid">
      <a href="https://globaldomotica.substack.com" target="_blank" class="btn btn-primary">
        Leer Capa Cero →
      </a>
      <button onclick="generarResumen()" class="btn btn-ghost">
        Ver mi resumen
      </button>
    </div>
    <div id="resumen-output" style="display:none;margin-top:16px;background:var(--surface2);border:1px solid var(--border);border-radius:4px;padding:20px;font-family:var(--mono);font-size:12px;color:var(--text-dim);line-height:1.9;"></div>
  </section>

  <footer>
    CAPA CERO · Infraestructura, robótica y soberanía del dato<br>
    globaldomotica.substack.com · Lo que sostiene tu hogar inteligente.
  </footer>

</div>

<script>
/* ── STATE ── */
let proto = 'wifi';
let tickerAccum = 0;
let modoContador = 'fuga';   // 'fuga' | 'soberania'
let _fugaMes = 0, _soberanMes = 0;
let tickRaf = null;

/* ── INTERSECTION OBSERVER (scroll reveal) ── */
const observer = new IntersectionObserver((entries) => {
  entries.forEach(e => {
    if (e.isIntersecting) {
      e.target.classList.add('visible');
      observer.unobserve(e.target);
    }
  });
}, { threshold: 0.12 });

document.querySelectorAll('section').forEach((s, i) => {
  s.style.transitionDelay = `${i * 0.06}s`;
  observer.observe(s);
});

/* ── MODO CONTADOR ── */
function setModoContador(modo) {
  modoContador = modo;
  tickerAccum  = 0;

  const btnFuga  = document.getElementById('btn-modo-fuga');
  const btnSober = document.getElementById('btn-modo-soberania') || document.getElementById('btn-modo-sober');
  const wrap     = document.getElementById('ticker-wrap-el');
  const label    = document.getElementById('ticker-label');

  if (modo === 'fuga') {
    btnFuga.style.background  = 'var(--danger)';
    btnFuga.style.color       = '#fff';
    btnSober.style.background = 'var(--surface2)';
    btnSober.style.color      = 'var(--text-dim)';
    wrap.style.borderColor    = 'var(--danger)';
    wrap.style.boxShadow      = 'var(--glow-red)';
    label.textContent         = '⬤ CAPITAL ESCAPANDO EN TIEMPO REAL';
    label.style.color         = 'var(--danger)';
    document.getElementById('ticker').parentElement.style.color = 'var(--danger)';
    document.getElementById('ticker').parentElement.style.textShadow = 'var(--glow-red)';
  } else {
    btnSober.style.background = 'var(--accent)';
    btnSober.style.color      = 'var(--bg)';
    btnFuga.style.background  = 'var(--surface2)';
    btnFuga.style.color       = 'var(--text-dim)';
    wrap.style.borderColor    = 'var(--accent)';
    wrap.style.boxShadow      = 'var(--glow-green)';
    label.textContent         = '🛡️ GASTO CON SOBERANÍA ACTIVA';
    label.style.color         = 'var(--accent)';
    document.getElementById('ticker').parentElement.style.color = 'var(--accent)';
    document.getElementById('ticker').parentElement.style.textShadow = 'var(--glow-green)';
  }

  iniciarTicker();
}

function iniciarTicker() {
  if (tickRaf) cancelAnimationFrame(tickRaf);
  tickerAccum = 0;
  const tasaMes = modoContador === 'fuga' ? _fugaMes : _soberanMes;
  const tasaSeg = tasaMes / (30 * 24 * 3600);
  let last = performance.now();

  function step(now) {
    const dt = (now - last) / 1000;
    last = now;
    tickerAccum += tasaSeg * dt;
    document.getElementById('ticker').textContent = tickerAccum.toFixed(6);
    tickRaf = requestAnimationFrame(step);
  }
  tickRaf = requestAnimationFrame(step);
}

/* ── GLOSARIO ── */
const glosario = [
  { t: "🧠 Home Assistant", d: "El cerebro local de tu casa. Corre en una Raspberry Pi, no en la nube de Amazon. Sin suscripción, sin caducidad, sin que nadie lea tus datos." },
  { t: "📡 Zigbee", d: "El idioma que hablan tus dispositivos entre sí. Más eficiente que el Wi-Fi, sin pasar por servidores externos. Si internet cae, Zigbee sigue funcionando." },
  { t: "🧛 Consumo Vampiro", d: "Lo que consumen tus aparatos en standby aunque estén 'apagados'. Un televisor moderno puede costarte 50€/año sin encenderse ni una vez." },
  { t: "☁️ Cloud Dependencia", d: "Cuando tu bombilla necesita hablar con un servidor en Virginia para encenderse. Sí, existe. Y es el modelo por defecto de casi todo lo que venden en el súper." },
  { t: "🔒 Soberanía Digital", d: "Que tu casa funcione si internet cae, si Amazon tiene un mal día, o si el fabricante cierra mañana. Tu casa, tus reglas, sin pedir permiso a nadie." },
  { t: "⚡ Local Push", d: "La automatización actúa en milisegundos porque el cerebro está en tu casa, a 2 metros, no en un datacenter a 5.000 km." },
];

const glosEl = document.getElementById('glossary');
glosario.forEach(g => {
  const d = document.createElement('div');
  d.className = 'glos-item';
  d.innerHTML = `<div class="glos-term">${g.t} <span class="caret">▼</span></div><div class="glos-def">${g.d}</div>`;
  d.onclick = () => d.classList.toggle('open');
  glosEl.appendChild(d);
});

/* ── PROTOCOLO ── */
function setProto(p) {
  proto = p;
  document.getElementById('proto-wifi').className   = p === 'wifi'   ? 'proto-btn selected-danger' : 'proto-btn';
  document.getElementById('proto-zigbee').className = p === 'zigbee' ? 'proto-btn selected'        : 'proto-btn';
  recalcular();
}

/* ── SLIDER LABEL ── */
document.getElementById('inflacion').addEventListener('input', e => {
  document.getElementById('inf-val').textContent = e.target.value + '%';
  recalcular();
});

['factura','estancias','nivel'].forEach(id =>
  document.getElementById(id).addEventListener('input', recalcular)
);

/* ── RECALCULAR ── */
function recalcular() {
  const factura   = parseFloat(document.getElementById('factura').value) || 0;
  const estancias = parseInt(document.getElementById('estancias').value) || 1;
  const nivelVal  = parseFloat(document.getElementById('nivel').value);
  const infPct    = parseFloat(document.getElementById('inflacion').value) / 100;

  const penalizacion = proto === 'wifi' ? 0.10 : 0.0;
  const ahorroPct    = Math.max(0, nivelVal - penalizacion);

  const facturaInflada = factura * (1 + infPct);
  const ahorroMensual  = facturaInflada * ahorroPct;
  const fugaMensual    = facturaInflada;                    // sin soberanía = factura inflada entera
  const soberMensual   = facturaInflada * (1 - ahorroPct); // con soberanía

  _fugaMes   = fugaMensual;
  _soberanMes = soberMensual;

  // Vitales
  document.getElementById('v-actual').textContent  = factura.toFixed(0);
  document.getElementById('v-inflada').textContent = facturaInflada.toFixed(0);
  document.getElementById('v-ahorro').textContent  = ahorroMensual.toFixed(0);

  // Comparativa dual
  document.getElementById('comp-fuga').textContent  = fugaMensual.toFixed(2);
  document.getElementById('comp-sober').textContent = soberMensual.toFixed(2);
  const diff = fugaMensual - soberMensual;
  document.getElementById('diff-label').textContent = `Diferencia: ${diff.toFixed(2)} €/mes · ${(diff*12).toFixed(0)} €/año`;
  const pct = ahorroPct > 0 ? Math.min(95, Math.round(ahorroPct * 100)) : 50;
  document.getElementById('diff-bar').style.width = pct + '%';

  // ticker sub
  const tasaActual = modoContador === 'fuga' ? fugaMensual : soberMensual;
  document.getElementById('ticker-sub').textContent =
    `${tasaActual.toFixed(2)} €/mes · ${(tasaActual*12).toFixed(0)} €/año`;

  // Veredicto
  const vb = document.getElementById('verdict-box');
  if (ahorroPct === 0) {
    vb.className = 'verdict verdict-danger';
    vb.innerHTML = `⚠️ Sin ningún nivel de automatización, el contador corre a máxima velocidad.<br>Estás regalando <strong>${(fugaMensual * 12).toFixed(0)}€/año</strong> sin saberlo.`;
  } else if (ahorroPct < 0.40) {
    vb.className = 'verdict verdict-warn';
    vb.innerHTML = `🔧 Nivel básico activo. Frenas un ${(ahorroPct*100).toFixed(0)}% de la fuga — pero hay margen significativo de mejora. La siguiente capa vale ${(facturaInflada * (0.75 - ahorroPct)).toFixed(0)}€/mes.`;
  } else {
    vb.className = 'verdict verdict-ok';
    vb.innerHTML = `🛡️ Nivel avanzado — frenas el ${(ahorroPct*100).toFixed(0)}% de la fuga. Tu casa trabaja para ti. Ahorro proyectado: <strong>${(ahorroMensual * 12).toFixed(0)}€/año</strong>.`;
  }

  iniciarTicker();
  drawChart(factura, infPct, ahorroPct, estancias);
  buildInventory(estancias);
}

/* ── GRÁFICA ── */
function drawChart(factura, infPct, ahorroPct, estancias) {
  const inversion = 250 + estancias * 50;
  const años = Array.from({length: 11}, (_, i) => i);
  const pasivo = [], soberano = [];
  let ap = 0, as_ = inversion;
  let mesCruce = -1;

  for (let m = 0; m <= 120; m++) {
    const yr = Math.floor(m / 12);
    const cm = factura * Math.pow(1 + infPct, yr);
    ap  += cm;
    as_ += cm * (1 - ahorroPct);
    if (mesCruce < 0 && as_ < ap && m > 0) mesCruce = m;
    if (m % 12 === 0) {
      pasivo.push(Math.round(ap));
      soberano.push(Math.round(as_));
    }
  }

  const ahorro10 = pasivo[10] - soberano[10];

  document.getElementById('big-saving').textContent =
    ahorro10 > 0 ? `${ahorro10.toLocaleString('es-ES')} €` : '—';
  document.getElementById('inv-estimada').textContent = `~${inversion}€`;

  // ROI box
  const roiEl = document.getElementById('roi-text');
  if (mesCruce > 0 && ahorroPct > 0) {
    roiEl.textContent = `Punto de cruce: mes ${mesCruce}. La inversión (~${inversion}€) se paga sola — en 10 años ahorras ${ahorro10.toLocaleString('es-ES')}€.`;
    document.getElementById('roi-box').style.borderColor = 'var(--accent)';
  } else if (ahorroPct === 0) {
    roiEl.textContent = `Sin automatización no hay cruce posible. El gasto sube en vertical.`;
    document.getElementById('roi-box').style.borderColor = 'var(--danger)';
    document.getElementById('roi-box').style.background  = 'rgba(255,59,59,.06)';
    document.getElementById('roi-box').querySelector('.roi-icon').textContent = '📉';
  } else {
    roiEl.textContent = `Con este nivel, la inversión se amortiza en el mes ${mesCruce > 0 ? mesCruce : '—'}.`;
  }

  // Canvas
  const canvas = document.getElementById('chart');
  const ctx    = canvas.getContext('2d');
  const dpr    = window.devicePixelRatio || 1;
  const W = canvas.offsetWidth, H = 220;
  canvas.width  = W * dpr;
  canvas.height = H * dpr;
  ctx.scale(dpr, dpr);
  ctx.clearRect(0, 0, W, H);

  const pad = { top: 10, right: 10, bottom: 30, left: 52 };
  const cW  = W - pad.left - pad.right;
  const cH  = H - pad.top  - pad.bottom;
  const maxV = Math.max(...pasivo) * 1.05;

  function xPos(i)  { return pad.left + (i / 10) * cW; }
  function yPos(v)  { return pad.top  + cH * (1 - v / maxV); }

  // Grid lines
  ctx.strokeStyle = 'rgba(255,255,255,0.05)';
  ctx.lineWidth   = 1;
  for (let i = 0; i <= 5; i++) {
    const y = pad.top + (cH / 5) * i;
    ctx.beginPath(); ctx.moveTo(pad.left, y); ctx.lineTo(W - pad.right, y); ctx.stroke();
  }

  // Área pasivo
  ctx.beginPath();
  ctx.moveTo(xPos(0), yPos(0));
  años.forEach(i => ctx.lineTo(xPos(i), yPos(pasivo[i])));
  ctx.lineTo(xPos(10), pad.top + cH);
  ctx.lineTo(xPos(0),  pad.top + cH);
  ctx.closePath();
  const gradR = ctx.createLinearGradient(0, pad.top, 0, pad.top + cH);
  gradR.addColorStop(0,   'rgba(255,59,59,0.3)');
  gradR.addColorStop(1,   'rgba(255,59,59,0.03)');
  ctx.fillStyle = gradR; ctx.fill();

  ctx.beginPath();
  años.forEach(i => i === 0 ? ctx.moveTo(xPos(0), yPos(pasivo[0])) : ctx.lineTo(xPos(i), yPos(pasivo[i])));
  ctx.strokeStyle = '#ff3b3b'; ctx.lineWidth = 2; ctx.stroke();

  // Área soberana
  ctx.beginPath();
  ctx.moveTo(xPos(0), yPos(inversion));
  años.forEach(i => ctx.lineTo(xPos(i), yPos(soberano[i])));
  ctx.lineTo(xPos(10), pad.top + cH);
  ctx.lineTo(xPos(0),  pad.top + cH);
  ctx.closePath();
  const gradG = ctx.createLinearGradient(0, pad.top, 0, pad.top + cH);
  gradG.addColorStop(0,   'rgba(0,229,160,0.25)');
  gradG.addColorStop(1,   'rgba(0,229,160,0.03)');
  ctx.fillStyle = gradG; ctx.fill();

  ctx.beginPath();
  años.forEach(i => i === 0 ? ctx.moveTo(xPos(0), yPos(soberano[0])) : ctx.lineTo(xPos(i), yPos(soberano[i])));
  ctx.strokeStyle = '#00e5a0'; ctx.lineWidth = 2; ctx.stroke();

  // Eje X
  ctx.fillStyle   = 'rgba(255,255,255,0.3)';
  ctx.font        = '10px Space Mono, monospace';
  ctx.textAlign   = 'center';
  años.forEach(i => {
    ctx.fillText(`Año ${i}`, xPos(i), H - 8);
  });

  // Eje Y
  ctx.textAlign = 'right';
  for (let i = 0; i <= 4; i++) {
    const v = (maxV / 4) * i;
    const y = yPos(v);
    if (v > 0) ctx.fillText(`${Math.round(v/1000)}k€`, pad.left - 6, y + 4);
  }
}

/* ── INVENTARIO ── */
function buildInventory(estancias) {
  const items = [
    { emoji: '🧠', name: 'Nodo local (Raspberry Pi + Home Assistant)', note: 'El cerebro que no duerme ni pide suscripción', qty: '1' },
    { emoji: '📡', name: 'Dongle Zigbee coordinador', note: 'ej. Sonoff ZBDongle-E — tu propia antena', qty: '1' },
    { emoji: '💡', name: 'Bombillas Zigbee', note: 'ej. IKEA TRÅDFRI E27 — sin hub propietario', qty: String(estancias * 2) },
    { emoji: '🔌', name: 'Enchufes con medidor de consumo', note: 'ej. Sonoff S60ZBTPF — para cazar vampiros', qty: String(estancias + 1) },
    { emoji: '🌡️', name: 'Sensores temperatura / humedad', note: 'Detectar el ambiente sin espiar a Amazon', qty: String(estancias) },
  ];
  const el = document.getElementById('inventory');
  el.innerHTML = '';
  items.forEach(item => {
    const d = document.createElement('div');
    d.className = 'inv-item';
    d.innerHTML = `
      <div class="inv-emoji">${item.emoji}</div>
      <div>
        <div class="inv-name">${item.name}</div>
        <div class="inv-note">${item.note}</div>
      </div>
      <div class="inv-qty">${item.qty}</div>
    `;
    el.appendChild(d);
  });
}

/* ── RESUMEN ── */
function generarResumen() {
  const factura   = parseFloat(document.getElementById('factura').value) || 0;
  const estancias = parseInt(document.getElementById('estancias').value) || 1;
  const nivelVal  = parseFloat(document.getElementById('nivel').value);
  const infPct    = parseFloat(document.getElementById('inflacion').value);
  const pen       = proto === 'wifi' ? 0.10 : 0.0;
  const ahorroPct = Math.max(0, nivelVal - pen);
  const factInflada = factura * (1 + infPct/100);
  const ahorroMes   = factInflada * ahorroPct;
  const inversion   = 250 + estancias * 50;
  const ahorro10    = parseInt(document.getElementById('big-saving').textContent.replace(/\D/g,'')) || 0;

  const el = document.getElementById('resumen-output');
  el.style.display = 'block';
  el.innerHTML = `
<strong style="color:var(--accent)">// RESUMEN · DIAGNÓSTICO CAPA CERO</strong><br><br>
FACTURA ACTUAL    → ${factura} €/mes<br>
INFLACIÓN         → ${infPct}% anual<br>
FACTURA AÑO 1     → ${factInflada.toFixed(0)} €/mes<br>
AHORRO POTENCIAL  → ${ahorroMes.toFixed(0)} €/mes (${(ahorroPct*100).toFixed(0)}% de freno)<br>
PROTOCOLO         → ${proto === 'wifi' ? '⚠️ Cloud (penalización activa)' : '✅ Local (Zigbee/Thread)'}<br>
INVERSIÓN EST.    → ~${inversion} €<br>
AHORRO A 10 AÑOS  → ${ahorro10.toLocaleString('es-ES')} €<br><br>
<strong style="color:var(--text-dim)">// Siguiente paso: globaldomotica.substack.com</strong>
  `;
}

/* ── INIT ── */
recalcular();
window.addEventListener('resize', recalcular);

// Forzar visible las primeras secciones sin esperar scroll
setTimeout(() => {
  document.querySelectorAll('section').forEach((s, i) => {
    if (i < 2) s.classList.add('visible');
  });
}, 80);
</script>
</body>
</html>
